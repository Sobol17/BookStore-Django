# Libra Antiqua — обновлённый UI шаблон

## 🎯 Цель проекта
Создать базовый шаблон интернет-магазина редких книг с атмосферой букинистического салона, но с современными UX-практиками: адаптивная сетка, быстрый поиск и акцент на карточки товаров.

---

## 🧭 Главное в обновлении
- Единый шаблон `main/base.html` управляет тремя сценариями: главная, каталог и карточка товара — переключение происходит через наличие `products` или `object` в контексте.
- Шапка получила новый поиск, кнопки для каталога/входа и иконки Lucide; мобильная версия раскрывается через Alpine.js.
- Весь контент завязан на Tailwind с преднастроенной палитрой (см. ниже), лёгкими тенями `shadow-lamplight` и светлой «бумажной» фактурой.
- HTMX отвечает за обновление контента без перезагрузки (каталог, фильтры, модальные окна через `#modal-root`).

---

## 🎨 Цветовая палитра
| Роль | Цвет | Использование |
|------|------|---------------|
| **Базовый фон** | `#F5F7FA` | Цвет «бумаги» для body и контейнеров |
| **Основной текст (`ink`)** | `#1F2937` | Заголовки, основной текст |
| **Вторичный текст (`ink-muted`)** | `#6B7280` | Подписи, описания, плейсхолдеры |
| **Акцент (`accent`)** | `#3B82F6` | CTA, активные элементы, плашки |
| **Акцент мягкий (`accent-soft`)** | `#DBEAFE` | Фоны карточек, градиенты, подсветка | 
| **Темный фон (`coffee`)** | `#111827` | Хедер, футер, мобильное меню |

*Цвета заведены в Tailwind-конфиг через встроенный объект `tailwind.config` в `<head>`.*

---

## ✍️ Типографика
- **Заголовки:** `Playfair Display` (через Google Fonts), подключены веса 400–700.
- **Основной текст:** `Inter` для интерфейса и описаний.
- Набор подгружается в `<head>` с предустановленными fallback-шрифтами.

---

## 🧱 Сценарии страницы
### Главная (нет `products` и `object`)
- Херо-блок с CTA-кнопками, иллюстрацией (`static/images/demo-cover-1.jpg`) и фоном-градиентом.
- Блок «Коллекции» отображает категории, если они переданы в контексте.
- Секция `#about` рассказывает о магазине и содержит форму «Экспресс-консультация`.

### Каталог (`products` в контексте)
- Заголовок с текущей категорией/результатами поиска.
- Слева фильтры (цена, год, автор) и список жанров; справа сетка карточек книг.
- Поиск и фильтры реагируют на HTMX-запросы от форм (ожидаются парциальные шаблоны `catalog.html`, `filter_modal.html` и др.).

### Карточка товара (`object` в контексте)
- Левая колонка: обложка, технические детали (состояние, ISBN, год, издатель, наличие).
- Правая колонка: название, автор(ы), стоимость, кнопка «Запросить экземпляр», описание, рендерится через `{{ object.description|linebreaks }}`.
- Блок похожих изданий (`related_products`) формирует сетку из четырёх карточек.

---

## 🧰 Ключевые компоненты
- **Хедер:** логотип, кнопка «Каталог», строка поиска, иконки Lucide (`heart`, `shopping-cart`, `circle-user-round`), мобильное меню на Alpine.js. При наличии `categories` добавляется горизонтальный бар с фильтром по категориям.
- **Карточки книг:** универсальный компонент для каталога и блоков «похожие товары» с плавными ховерами и акцентными бейджами.
- **Фильтры:** формы с Tailwind-классами и интерактивностью HTMX (`hx-target`, `hx-swap`). Значения фильтров прокидываются через `filter_params`.
- **Модальные окна:** контейнер `#modal-root` внизу `base.html` предназначен для загрузки HTMX-шаблонов (например, фильтра на мобильных устройствах).
- **Футер:** три колонки (описание, контакты, навигация) и подвал с копирайтом. Ссылки используют `hx-boost="true"` для плавной навигации.

---

## ⚙️ Технологический стек
- **Django Templates** с условными блоками для сценариев страницы.
- **Tailwind CSS (CDN)** с расширенной темой через `tailwind.config` в `<script>`.
- **HTMX** для частичной перезагрузки поискового поля, фильтра и каталога.
- **Alpine.js** управляет мобильным меню (`x-data`, `x-show`, `x-cloak`).
- **Lucide** предоставляет SVG-иконки, инициализация через `lucide.createIcons()`.

---

## 📂 Структура шаблонов
- `bookstore/main/templates/main/base.html` — корневой шаблон, который сразу отдаёт рабочий интерфейс. Остальные HTMX-шаблоны (`home_content.html`, `catalog.html`, `product_detail.html`, `filter_modal.html`, `search_input.html`, `search_button.html`) предполагаются для частичных рендеров и должны быть добавлены при развитии проекта.

---

## 🚀 Быстрый старт
1. Убедись, что в Django включены каталоги шаблонов и статики:
   ```python
   TEMPLATES[0]['DIRS'] = [BASE_DIR / 'templates']
   STATICFILES_DIRS = [BASE_DIR / 'static']
   ```
2. Создай структуру `templates/main/` и положи туда `base.html` (и при необходимости HTMX-парциалы).
3. Добавь статический ресурс `static/images/demo-cover-1.jpg` или замени на свою иллюстрацию.
4. В `urls.py` привяжи вьюхи `IndexView`, `CatalogView`, `ProductDetailView` к соответствующим маршрутами.
5. Запусти сервер (`python manage.py runserver`) и открой главную страницу — базовый шаблон уже включает хедер, футер, карточки и форму обратной связи.

---

## 🤖 DeepSeek-генерация рецензий
Чтобы блок «Рецензия» на карточке товара мог получать текст от DeepSeek:

- Добавь в `.env` значения `DEEPSEEK_API_KEY=<токен>` и при необходимости переопредели `DEEPSEEK_API_URL`, `DEEPSEEK_MODEL`, `DEEPSEEK_TIMEOUT`.
- После настройки кнопка «Получить» отправляет HTMX-запрос на `/product/<slug>/reviews/deepseek/`, который вызывает DeepSeek API и возвращает готовый текст.
- При отсутствии ключа кнопка отключается, а пользователю показывается подсказка о недоступности функции.

© 2025 Libra Antiqua — Обновлённый шаблон сайта букинистического магазина.
